import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  dark: {
    colors: {
      black: "#141414",
      white: "#F2F2F2",
      red: "#cc0811",
      grey: "#6D6D6E",
    },
  },

  white: {},
});

export { theme };
